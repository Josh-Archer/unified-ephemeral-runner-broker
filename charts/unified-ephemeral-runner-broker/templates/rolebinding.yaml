apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "uecb.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "uecb.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "uecb.fullname" . }}
