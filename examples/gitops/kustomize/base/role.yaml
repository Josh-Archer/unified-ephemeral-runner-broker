apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: unified-ephemeral-runner-broker
  namespace: arc-systems
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
