#!/usr/bin/env sh
set -eu

echo "Starting unified ephemeral runner bootstrap"
echo "This image is a generic placeholder for backend-specific one-shot runner bootstrap."
echo "Configure backend adapter launchers to provide JIT registration material at runtime."

